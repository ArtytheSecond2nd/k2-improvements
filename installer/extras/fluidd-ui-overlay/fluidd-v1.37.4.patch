diff --git a/components.d.ts b/components.d.ts
index 18415da..7a49e7c 100644
--- a/components.d.ts
+++ b/components.d.ts
@@ -61,10 +61,13 @@ declare module 'vue' {
     BedScrewsAdjustDialog: typeof import('./src/components/common/BedScrewsAdjustDialog.vue')['default']
     CollapsableCard: typeof import('./src/components/common/CollapsableCard.vue')['default']
     FlashMessage: typeof import('./src/components/common/FlashMessage.vue')['default']
+    GlobalTouchOffsetsDialog: typeof import('./src/components/common/GlobalTouchOffsetsDialog.vue')['default']
     KeyboardShortcutsDialog: typeof import('./src/components/common/KeyboardShortcutsDialog.vue')['default']
     KlippyStatusCard: typeof import('./src/components/common/KlippyStatusCard.vue')['default']
     Login: typeof import('./src/components/common/Login.vue')['default']
+    M191SettingsDialog: typeof import('./src/components/common/M191SettingsDialog.vue')['default']
     ManualProbeDialog: typeof import('./src/components/common/ManualProbeDialog.vue')['default']
+    MaterialZOffsetsDialog: typeof import('./src/components/common/MaterialZOffsetsDialog.vue')['default']
     PeripheralsDialog: typeof import('./src/components/common/PeripheralsDialog.vue')['default']
     PromiseWrapper: typeof import('./src/components/common/PromiseWrapper.vue')['default']
     RegisterServiceWorker: typeof import('./src/components/common/RegisterServiceWorker.vue')['default']
@@ -82,6 +85,8 @@ declare module 'vue' {
     VApp: typeof import('vuetify/lib')['VApp']
     VAppBar: typeof import('vuetify/lib')['VAppBar']
     VBadge: typeof import('vuetify/lib')['VBadge']
+    VBreadcrumbs: typeof import('vuetify/lib')['VBreadcrumbs']
+    VBreadcrumbsItem: typeof import('vuetify/lib')['VBreadcrumbsItem']
     VBtn: typeof import('vuetify/lib')['VBtn']
     VBtnToggle: typeof import('vuetify/lib')['VBtnToggle']
     VCard: typeof import('vuetify/lib')['VCard']
diff --git a/public/k2-ui-overlay-support.txt b/public/k2-ui-overlay-support.txt
new file mode 100644
index 0000000..b8626c4
--- /dev/null
+++ b/public/k2-ui-overlay-support.txt
@@ -0,0 +1 @@
+4
diff --git a/src/App.vue b/src/App.vue
index 2344eab..7a14b40 100644
--- a/src/App.vue
+++ b/src/App.vue
@@ -68,6 +68,9 @@
         <updating-dialog />
         <spool-selection-dialog />
         <action-command-prompt-dialog />
+        <global-touch-offsets-dialog />
+        <material-z-offsets-dialog />
+        <m191-settings-dialog />
         <keyboard-shortcuts-dialog />
         <manual-probe-dialog />
         <bed-screws-adjust-dialog />
@@ -100,6 +103,9 @@ import type { FlashMessage } from '@/types'
 import { getFilesFromDataTransfer, hasFilesInDataTransfer } from '@/util/file-system-entry'
 import type { ThemeConfig } from '@/store/config/types'
 import ActionCommandPromptDialog from '@/components/common/ActionCommandPromptDialog.vue'
+import GlobalTouchOffsetsDialog from '@/components/common/GlobalTouchOffsetsDialog.vue'
+import MaterialZOffsetsDialog from '@/components/common/MaterialZOffsetsDialog.vue'
+import M191SettingsDialog from '@/components/common/M191SettingsDialog.vue'
 import KeyboardShortcutsDialog from '@/components/common/KeyboardShortcutsDialog.vue'
 import { eventTargetIsContentEditable, keyboardEventToKeyboardShortcut } from '@/util/event-helpers'
 import MmuEditTtgMapDialog from './components/widgets/mmu/MmuEditTtgMapDialog.vue'
@@ -117,6 +123,9 @@ import MmuEditTtgMapDialog from './components/widgets/mmu/MmuEditTtgMapDialog.vu
     FileSystemDownloadDialog,
     FileSystemUploadDialog,
     ActionCommandPromptDialog,
+    GlobalTouchOffsetsDialog,
+    MaterialZOffsetsDialog,
+    M191SettingsDialog,
     KeyboardShortcutsDialog,
     MmuEditTtgMapDialog
   }
diff --git a/src/components/common/GlobalTouchOffsetsDialog.vue b/src/components/common/GlobalTouchOffsetsDialog.vue
new file mode 100644
index 0000000..0ae6307
--- /dev/null
+++ b/src/components/common/GlobalTouchOffsetsDialog.vue
@@ -0,0 +1,159 @@
+<template>
+  <app-dialog
+    v-model="open"
+    title="Global Carto Touch Z Offsets"
+    cancel-button-text="Cancel"
+    save-button-text="Save & Restart"
+    max-width="450"
+    @cancel="cancel"
+    @save="save"
+  >
+    <v-card-text>
+      <div class="text-body-2 mb-3">
+        Select a Touch model and increment. Bed down makes the offset more negative.
+      </div>
+
+      <v-row
+        v-for="model in models"
+        :key="model.index"
+        dense
+      >
+        <v-col>
+          <app-btn
+            :color="selectedModelIndex === model.index ? 'primary' : undefined"
+            :outlined="selectedModelIndex !== model.index"
+            block
+            class="model-button"
+            @click="selectedModelIndex = model.index"
+          >
+            <span>{{ model.name }}</span>
+            <v-spacer />
+            <span>{{ formatOffset(workingOffsets[model.index]) }} mm</span>
+          </app-btn>
+        </v-col>
+      </v-row>
+
+      <v-row dense>
+        <v-col>
+          <app-btn-toggle
+            v-model="selectedIncrement"
+            mandatory
+            class="d-flex"
+          >
+            <app-btn
+              v-for="increment in increments"
+              :key="increment"
+              :value="increment"
+              class="flex-grow-1"
+              small
+            >
+              {{ increment.toFixed(3) }}
+            </app-btn>
+          </app-btn-toggle>
+        </v-col>
+      </v-row>
+
+      <v-row dense>
+        <v-col>
+          <app-btn
+            color="primary"
+            block
+            @click="moveBedUp"
+          >
+            Bed up / closer
+          </app-btn>
+        </v-col>
+        <v-col>
+          <app-btn
+            color="secondary"
+            block
+            @click="moveBedDown"
+          >
+            Bed down / farther
+          </app-btn>
+        </v-col>
+      </v-row>
+    </v-card-text>
+  </app-dialog>
+</template>
+
+<script lang="ts">
+import Vue from 'vue'
+import { Component, Mixins, Watch } from 'vue-property-decorator'
+import StateMixin from '@/mixins/state'
+import type { GlobalTouchOffsetModel } from '@/store/console/types'
+import { adjustTouchOffset, buildTouchOffsetSaveCommands } from '@/util/global-touch-offsets'
+
+@Component({})
+export default class GlobalTouchOffsetsDialog extends Mixins(StateMixin) {
+  readonly increments = [0.005, 0.01, 0.025, 0.05]
+  selectedIncrement = 0.01
+  selectedModelIndex = 0
+  workingOffsets: Record<number, number> = {}
+
+  get dialog () {
+    return this.$typedState.console.globalTouchOffsetsDialog
+  }
+
+  get models (): GlobalTouchOffsetModel[] {
+    return this.dialog.models
+  }
+
+  get open (): boolean {
+    return this.dialog.open
+  }
+
+  set open (value: boolean) {
+    if (!value) this.cancel()
+  }
+
+  @Watch('dialog.open')
+  onDialogOpen (open: boolean) {
+    if (!open || this.models.length === 0) return
+
+    this.workingOffsets = {}
+    this.models.forEach(model => {
+      Vue.set(this.workingOffsets, model.index, model.value)
+    })
+    this.selectedModelIndex = this.models[0].index
+    this.selectedIncrement = 0.01
+  }
+
+  formatOffset (value: number): string {
+    return Number(value).toFixed(3)
+  }
+
+  moveBedUp () {
+    this.adjustSelected(this.selectedIncrement)
+  }
+
+  moveBedDown () {
+    this.adjustSelected(-this.selectedIncrement)
+  }
+
+  adjustSelected (delta: number) {
+    const value = this.workingOffsets[this.selectedModelIndex]
+    if (value === undefined) return
+    Vue.set(
+      this.workingOffsets,
+      this.selectedModelIndex,
+      adjustTouchOffset(value, delta)
+    )
+  }
+
+  cancel () {
+    this.sendGcode('K2_CARTOGRAPHER_GLOBAL_Z_CANCEL')
+  }
+
+  save () {
+    const commands = buildTouchOffsetSaveCommands(this.models, this.workingOffsets)
+    this.sendGcode(commands.join('\n'))
+  }
+}
+</script>
+
+<style lang="scss" scoped>
+.model-button {
+  text-transform: none;
+}
+</style>
diff --git a/src/components/common/M191SettingsDialog.vue b/src/components/common/M191SettingsDialog.vue
new file mode 100644
index 0000000..8d50a39
--- /dev/null
+++ b/src/components/common/M191SettingsDialog.vue
@@ -0,0 +1,208 @@
+<template>
+  <app-dialog
+    v-model="open"
+    title="Bed Assist"
+    max-width="540"
+    @cancel="cancel"
+    @save="save"
+  >
+    <v-card-text class="settings-scroll-area py-3">
+      <v-row
+        v-for="setting in settings"
+        :key="setting.index"
+        dense
+      >
+        <v-col>
+          <app-btn
+            :color="selectedSettingIndex === setting.index ? 'primary' : undefined"
+            :outlined="selectedSettingIndex !== setting.index"
+            block
+            class="setting-button py-2"
+            @click="selectedSettingIndex = setting.index"
+          >
+            <span class="setting-label text-left">
+              <span>{{ setting.label }}</span>
+              <small
+                v-if="setting.kind !== 'boolean'"
+                class="d-block range-label"
+              >
+                {{ formatRange(setting) }}
+              </small>
+            </span>
+            <v-spacer />
+            <span>{{ formatValue(setting, workingValues[setting.index]) }}</span>
+          </app-btn>
+        </v-col>
+      </v-row>
+    </v-card-text>
+
+    <template #actions>
+      <div class="editor-actions">
+        <app-btn-toggle
+          v-model="selectedIncrement"
+          mandatory
+          class="d-flex mb-2"
+        >
+          <app-btn
+            v-for="increment in increments"
+            :key="increment"
+            :value="increment"
+            class="flex-grow-1"
+            small
+          >
+            {{ increment }}
+          </app-btn>
+        </app-btn-toggle>
+
+        <div class="d-flex align-center">
+          <app-btn
+            color="secondary"
+            class="flex-grow-1 mr-2"
+            @click="adjustSelected(-selectedIncrement)"
+          >
+            <v-icon left>
+              $chevronDown
+            </v-icon>
+            Down
+          </app-btn>
+          <app-btn
+            color="primary"
+            class="flex-grow-1 mr-2"
+            @click="adjustSelected(selectedIncrement)"
+          >
+            <v-icon left>
+              $chevronUp
+            </v-icon>
+            Up
+          </app-btn>
+          <app-btn
+            color="warning"
+            text
+            @click="cancel"
+          >
+            Cancel
+          </app-btn>
+          <app-btn
+            color="primary"
+            @click="save"
+          >
+            Save & Restart
+          </app-btn>
+        </div>
+      </div>
+    </template>
+  </app-dialog>
+</template>
+
+<script lang="ts">
+import Vue from 'vue'
+import { Component, Mixins, Watch } from 'vue-property-decorator'
+import StateMixin from '@/mixins/state'
+import type { M191Setting } from '@/store/console/types'
+import { adjustM191Setting, buildM191SettingsSaveCommands } from '@/util/m191-settings'
+
+@Component({})
+export default class M191SettingsDialog extends Mixins(StateMixin) {
+  readonly increments = [1, 5, 10]
+  selectedIncrement = 1
+  selectedSettingIndex = 0
+  workingValues: Record<number, number> = {}
+
+  get dialog () {
+    return this.$typedState.console.m191SettingsDialog
+  }
+
+  get settings (): M191Setting[] {
+    return this.dialog.settings
+  }
+
+  get open (): boolean {
+    return this.dialog.open
+  }
+
+  set open (value: boolean) {
+    if (!value) this.cancel()
+  }
+
+  @Watch('dialog.open')
+  onDialogOpen (open: boolean) {
+    if (!open || this.settings.length === 0) return
+    this.workingValues = {}
+    this.settings.forEach(setting => {
+      Vue.set(this.workingValues, setting.index, setting.value)
+    })
+    this.selectedSettingIndex = this.settings[0].index
+    this.selectedIncrement = 1
+  }
+
+  formatRange (setting: M191Setting): string {
+    const minimum = setting.exclusiveMinimum ? `>${setting.minimum}` : `${setting.minimum}`
+    return `${minimum}-${setting.maximum}${this.formatUnit(setting.unit)}`
+  }
+
+  formatValue (setting: M191Setting, value: number): string {
+    if (setting.kind === 'boolean') return value === 1 ? 'Enabled' : 'Disabled'
+    return `${value}${this.formatUnit(setting.unit)}`
+  }
+
+  formatUnit (unit: string): string {
+    return unit === 'C' ? '°C' : unit
+  }
+
+  adjustSelected (delta: number) {
+    const setting = this.settings.find(item => item.index === this.selectedSettingIndex)
+    if (!setting) return
+    const value = this.workingValues[setting.index]
+    Vue.set(
+      this.workingValues,
+      setting.index,
+      adjustM191Setting(setting, value, delta)
+    )
+  }
+
+  cancel () {
+    this.sendGcode('K2_M191_SETTINGS_CANCEL')
+  }
+
+  save () {
+    const commands = buildM191SettingsSaveCommands(this.settings, this.workingValues)
+    this.sendGcode(commands.join('\n'))
+  }
+}
+</script>
+
+<style lang="scss" scoped>
+.settings-scroll-area {
+  max-height: min(58vh, 520px);
+  overflow-y: auto;
+}
+
+.setting-button {
+  height: auto !important;
+  min-height: 52px;
+  text-transform: none;
+}
+
+.setting-label {
+  line-height: 1.2;
+}
+
+.range-label {
+  opacity: 0.72;
+}
+
+.editor-actions {
+  width: 100%;
+}
+
+@media (max-width: 600px) {
+  .editor-actions > .d-flex:last-child {
+    flex-wrap: wrap;
+    gap: 8px;
+  }
+
+  .editor-actions > .d-flex:last-child > * {
+    margin-right: 0 !important;
+  }
+}
+</style>
diff --git a/src/components/common/MaterialZOffsetsDialog.vue b/src/components/common/MaterialZOffsetsDialog.vue
new file mode 100644
index 0000000..fa48c38
--- /dev/null
+++ b/src/components/common/MaterialZOffsetsDialog.vue
@@ -0,0 +1,165 @@
+<template>
+  <app-dialog
+    v-model="open"
+    title="Material Z Offsets"
+    cancel-button-text="Cancel"
+    save-button-text="Save & Restart"
+    max-width="450"
+    @cancel="cancel"
+    @save="save"
+  >
+    <v-card-text>
+      <div class="text-body-2 mb-3">
+        Bed up / closer makes the material offset more negative. Bed down / farther makes it more positive.
+      </div>
+
+      <v-row
+        v-for="material in materials"
+        :key="material.index"
+        dense
+      >
+        <v-col>
+          <app-btn
+            :color="selectedMaterialIndex === material.index ? 'primary' : undefined"
+            :outlined="selectedMaterialIndex !== material.index"
+            block
+            class="material-button"
+            @click="selectedMaterialIndex = material.index"
+          >
+            <span>{{ material.name }}</span>
+            <v-spacer />
+            <span>{{ formatOffset(workingOffsets[material.index]) }} mm</span>
+          </app-btn>
+        </v-col>
+      </v-row>
+
+      <v-row dense>
+        <v-col>
+          <app-btn-toggle
+            v-model="selectedIncrement"
+            mandatory
+            class="d-flex"
+          >
+            <app-btn
+              v-for="increment in increments"
+              :key="increment"
+              :value="increment"
+              class="flex-grow-1 px-1"
+              small
+            >
+              {{ increment.toFixed(3) }}
+            </app-btn>
+          </app-btn-toggle>
+        </v-col>
+      </v-row>
+
+      <v-row dense>
+        <v-col
+          cols="12"
+          sm="6"
+        >
+          <app-btn
+            color="primary"
+            block
+            @click="moveBedUp"
+          >
+            Bed up / closer
+          </app-btn>
+        </v-col>
+        <v-col
+          cols="12"
+          sm="6"
+        >
+          <app-btn
+            color="secondary"
+            block
+            @click="moveBedDown"
+          >
+            Bed down / farther
+          </app-btn>
+        </v-col>
+      </v-row>
+    </v-card-text>
+  </app-dialog>
+</template>
+
+<script lang="ts">
+import Vue from 'vue'
+import { Component, Mixins, Watch } from 'vue-property-decorator'
+import StateMixin from '@/mixins/state'
+import type { MaterialZOffset } from '@/store/console/types'
+import { adjustMaterialOffset, buildMaterialOffsetSaveCommands } from '@/util/material-z-offsets'
+
+@Component({})
+export default class MaterialZOffsetsDialog extends Mixins(StateMixin) {
+  readonly increments = [0.005, 0.01, 0.025, 0.05]
+  selectedIncrement = 0.01
+  selectedMaterialIndex = 0
+  workingOffsets: Record<number, number> = {}
+
+  get dialog () {
+    return this.$typedState.console.materialZOffsetsDialog
+  }
+
+  get materials (): MaterialZOffset[] {
+    return this.dialog.materials
+  }
+
+  get open (): boolean {
+    return this.dialog.open
+  }
+
+  set open (value: boolean) {
+    if (!value) this.cancel()
+  }
+
+  @Watch('dialog.open')
+  onDialogOpen (open: boolean) {
+    if (!open || this.materials.length === 0) return
+
+    this.workingOffsets = {}
+    this.materials.forEach(material => {
+      Vue.set(this.workingOffsets, material.index, material.value)
+    })
+    this.selectedMaterialIndex = this.materials[0].index
+    this.selectedIncrement = 0.01
+  }
+
+  formatOffset (value: number): string {
+    return Number(value).toFixed(3)
+  }
+
+  moveBedUp () {
+    this.adjustSelected(-this.selectedIncrement)
+  }
+
+  moveBedDown () {
+    this.adjustSelected(this.selectedIncrement)
+  }
+
+  adjustSelected (delta: number) {
+    const value = this.workingOffsets[this.selectedMaterialIndex]
+    if (value === undefined) return
+    Vue.set(
+      this.workingOffsets,
+      this.selectedMaterialIndex,
+      adjustMaterialOffset(value, delta)
+    )
+  }
+
+  cancel () {
+    this.sendGcode('K2_MATERIAL_Z_CANCEL')
+  }
+
+  save () {
+    const commands = buildMaterialOffsetSaveCommands(this.materials, this.workingOffsets)
+    this.sendGcode(commands.join('\n'))
+  }
+}
+</script>
+
+<style lang="scss" scoped>
+.material-button {
+  text-transform: none;
+}
+</style>
diff --git a/src/components/widgets/camera/services/WebrtcCrealityk2RtcCamera.vue b/src/components/widgets/camera/services/WebrtcCrealityk2RtcCamera.vue
new file mode 100644
index 0000000..b904777
--- /dev/null
+++ b/src/components/widgets/camera/services/WebrtcCrealityk2RtcCamera.vue
@@ -0,0 +1,126 @@
+<template>
+  <video
+    ref="streamingElement"
+    autoplay
+    disablePictureInPicture
+    playsinline
+    muted
+    :style="cameraStyle"
+    :crossorigin="crossorigin"
+    @play="updateStatus('connected')"
+    @error="onError"
+  />
+</template>
+
+<script lang="ts">
+import { Component, Mixins, Ref } from 'vue-property-decorator'
+import CameraMixin from '@/mixins/camera'
+import { consola } from 'consola'
+import sleep from '@/util/sleep'
+
+type RTCConfigurationWithSdpSemantics = RTCConfiguration & {
+  sdpSemantics: 'unified-plan'
+}
+
+const iceServers = [
+  { urls: 'stun:stun.l.google.com:19302' }
+]
+
+@Component({})
+export default class WebrtcCrealityk2RtcCamera extends Mixins(CameraMixin) {
+  @Ref('streamingElement')
+  readonly cameraVideo!: HTMLVideoElement
+
+  pc: RTCPeerConnection | null = null
+  playbackAbortController: AbortController | null = null
+  sleepAbortController: AbortController | null = null
+
+  async loadStream () {
+    this.pc?.close()
+
+    const signal = this.playbackAbortController?.signal
+    if (!signal || signal.aborted) return
+
+    this.updateStatus('connecting')
+    const url = this.buildAbsoluteUrl(this.camera.stream_url || '')
+    this.updateRawCameraUrl(url.toString())
+
+    try {
+      const config: RTCConfigurationWithSdpSemantics = {
+        sdpSemantics: 'unified-plan',
+        iceServers
+      }
+      const pc = this.pc = new RTCPeerConnection(config)
+
+      pc.onicecandidate = async event => {
+        if (event.candidate !== null || !pc.localDescription) return
+        try {
+          const response = await fetch(`${url}call/webrtc_local`, {
+            body: btoa(JSON.stringify({
+              type: 'offer',
+              sdp: pc.localDescription.sdp
+            })),
+            headers: { 'Content-Type': 'plain/text' },
+            method: 'POST',
+            signal
+          })
+          const answer = JSON.parse(
+            atob(await response.text())
+          ) as RTCSessionDescriptionInit
+          if (answer.type === 'answer') {
+            await pc.setRemoteDescription(new RTCSessionDescription(answer))
+          }
+        } catch (error) {
+          consola.error('[WebrtcCrealityk2RtcCamera] onicecandidate', error)
+          this.onError()
+        }
+      }
+
+      pc.addTransceiver('video', { direction: 'recvonly' })
+      pc.ontrack = event => {
+        if (event.track.kind === 'video') {
+          this.cameraVideo.srcObject = event.streams[0]
+        }
+      }
+
+      const offer = await pc.createOffer()
+      await pc.setLocalDescription(offer)
+      consola.debug('[WebrtcCrealityk2RtcCamera] Set Creality K2 Camera')
+    } catch (error) {
+      consola.error(`[WebrtcCrealityk2RtcCamera] failed to start playback "${this.camera.name}"`, error)
+      this.onError()
+    }
+  }
+
+  async onError () {
+    this.updateStatus('error')
+    this.pc?.close()
+    this.pc = null
+
+    const signal = this.playbackAbortController?.signal
+    if (!signal || signal.aborted) return
+
+    this.sleepAbortController?.abort()
+    const retry = this.sleepAbortController = new AbortController()
+    try {
+      await sleep(2000, AbortSignal.any([signal, retry.signal]))
+      this.loadStream()
+    } catch {}
+  }
+
+  async startPlayback () {
+    this.playbackAbortController = new AbortController()
+    await this.loadStream()
+  }
+
+  stopPlayback () {
+    this.updateStatus('disconnected')
+    this.playbackAbortController?.abort()
+    this.playbackAbortController = null
+    this.pc?.close()
+    this.pc = null
+    this.cameraVideo.src = ''
+    this.cameraVideo.srcObject = null
+  }
+}
+</script>
diff --git a/src/store/console/actions.ts b/src/store/console/actions.ts
index 90385cb..2464e80 100644
--- a/src/store/console/actions.ts
+++ b/src/store/console/actions.ts
@@ -1,6 +1,6 @@
 import type { ActionTree } from 'vuex'
 import { Globals } from '@/globals'
-import type { ConsoleEntry, ConsoleFilter, ConsoleState, PromptDialogButton, PromptDialogItemButton, PromptDialogItemText } from './types'
+import type { ConsoleEntry, ConsoleFilter, ConsoleState, GlobalTouchOffsetModel, M191Setting, MaterialZOffset, PromptDialogButton, PromptDialogItemButton, PromptDialogItemText } from './types'
 import type { RootState } from '../types'
 import { SocketActions } from '@/api/socketActions'
 import DOMPurify from 'dompurify'
@@ -57,6 +57,9 @@ export const actions = {
     commit('setConsoleEntry', payload)
 
     dispatch('onUpdatePromptDialog', payload)
+    dispatch('onUpdateGlobalTouchOffsetsDialog', payload)
+    dispatch('onUpdateMaterialZOffsetsDialog', payload)
+    dispatch('onUpdateM191SettingsDialog', payload)
   },
 
   /**
@@ -160,6 +163,118 @@ export const actions = {
     }
   },
 
+  async onUpdateGlobalTouchOffsetsDialog ({ commit }, payload: ConsoleEntry) {
+    const parsedMessage = (
+      payload.type === 'action' &&
+      /^\/\/ action:global_touch_offsets_([^ ]+)(?: (.+))?/.exec(payload.message)
+    )
+
+    if (!parsedMessage) return
+
+    const [, type, param] = parsedMessage
+    switch (type) {
+      case 'begin':
+        commit('setResetGlobalTouchOffsetsDialog')
+        break
+
+      case 'model': {
+        const [name, valueText, indexText] = param.split('|')
+        const value = Number(valueText)
+        const index = Number(indexText)
+        if (name && Number.isFinite(value) && Number.isInteger(index) && index >= 0) {
+          const model: GlobalTouchOffsetModel = { name, value, index }
+          commit('setGlobalTouchOffsetModel', model)
+        }
+        break
+      }
+
+      case 'show':
+      case 'end':
+        commit('setGlobalTouchOffsetsDialogOpen', type === 'show')
+        break
+    }
+  },
+
+  async onUpdateMaterialZOffsetsDialog ({ commit }, payload: ConsoleEntry) {
+    const parsedMessage = (
+      payload.type === 'action' &&
+      /^\/\/ action:material_z_offsets_([^ ]+)(?: (.+))?/.exec(payload.message)
+    )
+
+    if (!parsedMessage) return
+
+    const [, type, param] = parsedMessage
+    switch (type) {
+      case 'begin':
+        commit('setResetMaterialZOffsetsDialog')
+        break
+
+      case 'material': {
+        const [name, valueText, indexText] = param.split('|')
+        const value = Number(valueText)
+        const index = Number(indexText)
+        if (name && Number.isFinite(value) && Number.isInteger(index) && index >= 0) {
+          const material: MaterialZOffset = { name, value, index }
+          commit('setMaterialZOffset', material)
+        }
+        break
+      }
+
+      case 'show':
+      case 'end':
+        commit('setMaterialZOffsetsDialogOpen', type === 'show')
+        break
+    }
+  },
+
+  async onUpdateM191SettingsDialog ({ commit }, payload: ConsoleEntry) {
+    const parsedMessage = (
+      payload.type === 'action' &&
+      /^\/\/ action:m191_settings_([^ ]+)(?: (.+))?/.exec(payload.message)
+    )
+
+    if (!parsedMessage) return
+
+    const [, type, param] = parsedMessage
+    switch (type) {
+      case 'begin':
+        commit('setResetM191SettingsDialog')
+        break
+
+      case 'setting': {
+        const [key, label, valueText, minimumText, maximumText, unit, indexText, kind, exclusiveText] = param.split('|')
+        const value = Number(valueText)
+        const minimum = Number(minimumText)
+        const maximum = Number(maximumText)
+        const index = Number(indexText)
+        if (
+          key && label && Number.isFinite(value) && Number.isFinite(minimum) &&
+          Number.isFinite(maximum) && Number.isInteger(index) && index >= 0 &&
+          (kind === 'number' || kind === 'boolean')
+        ) {
+          const setting: M191Setting = {
+            key,
+            label,
+            value,
+            minimum,
+            maximum,
+            unit,
+            index,
+            kind,
+            exclusiveMinimum: exclusiveText === '1'
+          }
+          commit('setM191Setting', setting)
+        }
+        break
+      }
+
+      case 'show':
+      case 'end':
+        commit('setM191SettingsDialogOpen', type === 'show')
+        break
+    }
+  },
+
   /**
    * Klipper provides us with a list of available gcode commands
    * based on the current configuration.
diff --git a/src/store/console/mutations.ts b/src/store/console/mutations.ts
index 400e40b..0c1566e 100644
--- a/src/store/console/mutations.ts
+++ b/src/store/console/mutations.ts
@@ -3,7 +3,7 @@ import { v4 as uuidv4 } from 'uuid'
 import type { MutationTree } from 'vuex'
 import { defaultState } from './state'
 import { Globals } from '@/globals'
-import type { ConsoleEntry, ConsoleFilter, ConsoleState, PromptDialogItemButton, PromptDialogItem } from './types'
+import type { ConsoleEntry, ConsoleFilter, ConsoleState, GlobalTouchOffsetModel, M191Setting, MaterialZOffset, PromptDialogItemButton, PromptDialogItem } from './types'
 import { escapeRegExp } from 'lodash-es'
 
 const compileExpression = (filter: ConsoleFilter): RegExp => {
@@ -71,6 +71,51 @@ export const mutations = {
     state.promptDialog.open = payload
   },
 
+  setResetGlobalTouchOffsetsDialog (state) {
+    state.globalTouchOffsetsDialog = {
+      open: false,
+      models: []
+    }
+  },
+
+  setGlobalTouchOffsetModel (state, payload: GlobalTouchOffsetModel) {
+    state.globalTouchOffsetsDialog.models.push(payload)
+  },
+
+  setGlobalTouchOffsetsDialogOpen (state, payload: boolean) {
+    state.globalTouchOffsetsDialog.open = payload
+  },
+
+  setResetMaterialZOffsetsDialog (state) {
+    state.materialZOffsetsDialog = {
+      open: false,
+      materials: []
+    }
+  },
+
+  setMaterialZOffset (state, payload: MaterialZOffset) {
+    state.materialZOffsetsDialog.materials.push(payload)
+  },
+
+  setMaterialZOffsetsDialogOpen (state, payload: boolean) {
+    state.materialZOffsetsDialog.open = payload
+  },
+
+  setResetM191SettingsDialog (state) {
+    state.m191SettingsDialog = {
+      open: false,
+      settings: []
+    }
+  },
+
+  setM191Setting (state, payload: M191Setting) {
+    state.m191SettingsDialog.settings.push(payload)
+  },
+
+  setM191SettingsDialogOpen (state, payload: boolean) {
+    state.m191SettingsDialog.open = payload
+  },
+
   /**
    * Defines the list of available commands
    */
diff --git a/src/store/console/state.ts b/src/store/console/state.ts
index 417f8d8..56dc7b7 100644
--- a/src/store/console/state.ts
+++ b/src/store/console/state.ts
@@ -15,6 +15,18 @@ export const defaultState = (): ConsoleState => {
       items: [],
       footerButtons: []
     },
+    globalTouchOffsetsDialog: {
+      open: false,
+      models: []
+    },
+    materialZOffsetsDialog: {
+      open: false,
+      materials: []
+    },
+    m191SettingsDialog: {
+      open: false,
+      settings: []
+    },
     consoleFilters: [],
     consoleFiltersRegexp: []
   }
diff --git a/src/store/console/types.ts b/src/store/console/types.ts
index 2eddc92..bd6473c 100644
--- a/src/store/console/types.ts
+++ b/src/store/console/types.ts
@@ -9,6 +9,9 @@ export interface ConsoleState {
   autoScroll: boolean;
   lastCleared: number;
   promptDialog: PromptDialog;
+  globalTouchOffsetsDialog: GlobalTouchOffsetsDialog;
+  materialZOffsetsDialog: MaterialZOffsetsDialog;
+  m191SettingsDialog: M191SettingsDialog;
   consoleFilters: ConsoleFilter[];
   consoleFiltersRegexp: RegExp[];
 }
@@ -48,6 +51,45 @@ export interface PromptDialogItemButton extends PromptDialogButton {
   type: 'button';
 }
 
+export interface GlobalTouchOffsetModel {
+  index: number;
+  name: string;
+  value: number;
+}
+
+export interface GlobalTouchOffsetsDialog {
+  open: boolean;
+  models: GlobalTouchOffsetModel[];
+}
+
+export interface MaterialZOffset {
+  index: number;
+  name: string;
+  value: number;
+}
+
+export interface MaterialZOffsetsDialog {
+  open: boolean;
+  materials: MaterialZOffset[];
+}
+
+export interface M191Setting {
+  index: number;
+  key: string;
+  label: string;
+  value: number;
+  minimum: number;
+  maximum: number;
+  unit: string;
+  kind: 'number' | 'boolean';
+  exclusiveMinimum: boolean;
+}
+
+export interface M191SettingsDialog {
+  open: boolean;
+  settings: M191Setting[];
+}
+
 export interface PromptDialogButton {
   text: string;
   command?: string;
diff --git a/src/util/__tests__/global-touch-offsets.spec.ts b/src/util/__tests__/global-touch-offsets.spec.ts
new file mode 100644
index 0000000..f188ecb
--- /dev/null
+++ b/src/util/__tests__/global-touch-offsets.spec.ts
@@ -0,0 +1,24 @@
+import { adjustTouchOffset, buildTouchOffsetSaveCommands } from '../global-touch-offsets'
+
+describe('global touch offsets', () => {
+  it('moves up toward zero and prevents a positive touch offset', () => {
+    expect(adjustTouchOffset(-0.05, 0.025)).toBe(-0.025)
+    expect(adjustTouchOffset(-0.005, 0.01)).toBe(0)
+  })
+
+  it('moves down to a more-negative touch offset', () => {
+    expect(adjustTouchOffset(-0.05, -0.025)).toBe(-0.075)
+  })
+
+  it('stages only changed models before saving', () => {
+    const models = [
+      { index: 0, name: 'DEFAULT', value: -0.05 },
+      { index: 1, name: 'TEXTURED_PEI', value: -0.06 }
+    ]
+
+    expect(buildTouchOffsetSaveCommands(models, { 0: -0.075, 1: -0.06 })).toEqual([
+      'K2_CARTOGRAPHER_GLOBAL_Z_STAGE INDEX=0 VALUE=-0.075',
+      'K2_CARTOGRAPHER_GLOBAL_Z_SAVE'
+    ])
+  })
+})
diff --git a/src/util/__tests__/m191-settings.spec.ts b/src/util/__tests__/m191-settings.spec.ts
new file mode 100644
index 0000000..14e2049
--- /dev/null
+++ b/src/util/__tests__/m191-settings.spec.ts
@@ -0,0 +1,35 @@
+import { adjustM191Setting, buildM191SettingsSaveCommands } from '../m191-settings'
+
+const numeric = {
+  index: 1,
+  key: 'bed_assist_z_height',
+  label: 'Bed Z Height',
+  value: 195,
+  minimum: 30,
+  maximum: 330,
+  unit: 'mm',
+  kind: 'number' as const,
+  exclusiveMinimum: false
+}
+
+describe('M191 settings', () => {
+  it('adjusts and clamps numeric values', () => {
+    expect(adjustM191Setting(numeric, 195, 10)).toBe(205)
+    expect(adjustM191Setting(numeric, 325, 10)).toBe(330)
+    expect(adjustM191Setting({ ...numeric, minimum: 0, exclusiveMinimum: true }, 5, -10)).toBe(1)
+  })
+
+  it('uses down and up as disable and enable for the boolean setting', () => {
+    const setting = { ...numeric, kind: 'boolean' as const, minimum: 0, maximum: 1 }
+    expect(adjustM191Setting(setting, 1, -10)).toBe(0)
+    expect(adjustM191Setting(setting, 0, 10)).toBe(1)
+  })
+
+  it('stages only changed settings before saving', () => {
+    const settings = [numeric, { ...numeric, index: 2, key: 'circulation_fan_speed', value: 25 }]
+    expect(buildM191SettingsSaveCommands(settings, { 1: 205, 2: 25 })).toEqual([
+      'K2_M191_SETTINGS_STAGE INDEX=1 VALUE=205',
+      'K2_M191_SETTINGS_SAVE'
+    ])
+  })
+})
diff --git a/src/util/__tests__/material-z-offsets.spec.ts b/src/util/__tests__/material-z-offsets.spec.ts
new file mode 100644
index 0000000..8d1dc9a
--- /dev/null
+++ b/src/util/__tests__/material-z-offsets.spec.ts
@@ -0,0 +1,24 @@
+import { adjustMaterialOffset, buildMaterialOffsetSaveCommands } from '../material-z-offsets'
+
+describe('material Z offsets', () => {
+  it('moves closer in the negative direction and farther in the positive direction', () => {
+    expect(adjustMaterialOffset(0.05, -0.01)).toBe(0.04)
+    expect(adjustMaterialOffset(0.05, 0.025)).toBe(0.075)
+  })
+
+  it('limits values to the backend range', () => {
+    expect(adjustMaterialOffset(-4.99, -0.05)).toBe(-5)
+    expect(adjustMaterialOffset(4.99, 0.05)).toBe(5)
+  })
+
+  it('stages only changed rows and then saves', () => {
+    const commands = buildMaterialOffsetSaveCommands(
+      [{ index: 0, value: 0.05 }, { index: 1, value: -0.02 }],
+      { 0: 0.075, 1: -0.02 }
+    )
+    expect(commands).toEqual([
+      'K2_MATERIAL_Z_STAGE INDEX=0 VALUE=0.075',
+      'K2_MATERIAL_Z_SAVE'
+    ])
+  })
+})
diff --git a/src/util/global-touch-offsets.ts b/src/util/global-touch-offsets.ts
new file mode 100644
index 0000000..8370d98
--- /dev/null
+++ b/src/util/global-touch-offsets.ts
@@ -0,0 +1,24 @@
+interface TouchOffsetModel {
+  index: number;
+  value: number;
+}
+
+export const adjustTouchOffset = (value: number, delta: number): number => {
+  const adjusted = Math.round((value + delta) * 1000) / 1000
+  return Math.min(0, adjusted)
+}
+
+export const buildTouchOffsetSaveCommands = (
+  models: TouchOffsetModel[],
+  workingOffsets: Record<number, number>
+): string[] => {
+  const commands = models
+    .filter(model => workingOffsets[model.index] !== model.value)
+    .map(model => (
+      `K2_CARTOGRAPHER_GLOBAL_Z_STAGE INDEX=${model.index} ` +
+      `VALUE=${workingOffsets[model.index].toFixed(3)}`
+    ))
+
+  commands.push('K2_CARTOGRAPHER_GLOBAL_Z_SAVE')
+  return commands
+}
diff --git a/src/util/m191-settings.ts b/src/util/m191-settings.ts
new file mode 100644
index 0000000..ab73d14
--- /dev/null
+++ b/src/util/m191-settings.ts
@@ -0,0 +1,28 @@
+import type { M191Setting } from '@/store/console/types'
+
+export const adjustM191Setting = (
+  setting: M191Setting,
+  value: number,
+  delta: number
+): number => {
+  if (setting.kind === 'boolean') return delta > 0 ? 1 : 0
+  const lowerBound = setting.exclusiveMinimum
+    ? Math.min(setting.minimum + 1, setting.maximum)
+    : setting.minimum
+  const adjusted = Math.round((value + delta) * 1000) / 1000
+  return Math.max(lowerBound, Math.min(setting.maximum, adjusted))
+}
+
+export const buildM191SettingsSaveCommands = (
+  settings: M191Setting[],
+  workingValues: Record<number, number>
+): string[] => {
+  const commands = settings
+    .filter(setting => workingValues[setting.index] !== setting.value)
+    .map(setting => (
+      `K2_M191_SETTINGS_STAGE INDEX=${setting.index} ` +
+      `VALUE=${workingValues[setting.index]}`
+    ))
+  commands.push('K2_M191_SETTINGS_SAVE')
+  return commands
+}
diff --git a/src/util/material-z-offsets.ts b/src/util/material-z-offsets.ts
new file mode 100644
index 0000000..78054ba
--- /dev/null
+++ b/src/util/material-z-offsets.ts
@@ -0,0 +1,24 @@
+interface MaterialOffset {
+  index: number;
+  value: number;
+}
+
+export const adjustMaterialOffset = (value: number, delta: number): number => {
+  const adjusted = Math.round((value + delta) * 1000) / 1000
+  return Math.max(-5, Math.min(5, adjusted))
+}
+
+export const buildMaterialOffsetSaveCommands = (
+  materials: MaterialOffset[],
+  workingOffsets: Record<number, number>
+): string[] => {
+  const commands = materials
+    .filter(material => workingOffsets[material.index] !== material.value)
+    .map(material => (
+      `K2_MATERIAL_Z_STAGE INDEX=${material.index} ` +
+      `VALUE=${workingOffsets[material.index].toFixed(3)}`
+    ))
+
+  commands.push('K2_MATERIAL_Z_SAVE')
+  return commands
+}
